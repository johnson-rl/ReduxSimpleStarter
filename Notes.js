Container - React Component Managed Directly by redux

ReactRedux - Package for combining the two.

Containers should be made out of the most parent component that compares about a particular piece of state

connect - takes a function and a component as arguments and produces a container

every time app state changes, object  in state funciont is assigned as propse to the component.

Container is a Component that is aware of the app state maintained by redux
